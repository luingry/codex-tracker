# Lucide source assets

Source: https://lucide.dev and https://github.com/lucide-icons/lucide. Icons used: `settings`, `pin`, and `x`; stored as original SVG paths for provenance and converted to WPF `Path.Data` in the application.

